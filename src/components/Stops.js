import React from "react";
import {
  GoogleMap,
  useLoadScript,
  Marker,
  InfoWindow,
} from "@react-google-maps/api";

export default function Stops(props) {
  return props.data.map((stop) => (
    <>
     
    </>
  ));
}
